---
export interface Props {
  genres: string[];
}

const { genres } = Astro.props;
---

<section class="py-6 px-4 bg-linear-to-r from-blue-50 to-purple-50">
  <div class="max-w-6xl mx-auto">
    <h3 class="text-lg font-semibold mb-4 text-center text-gray-700">Filtrar por Género</h3>
    <div class="flex flex-wrap justify-center gap-3">
      {genres.map(genre => (
        <span class="px-4 py-2 bg-white bg-opacity-80 backdrop-blur-sm rounded-full shadow-md text-sm font-medium text-gray-700 hover:bg-blue-100 hover:text-blue-800 transition-colors cursor-pointer">{genre}</span>
      ))}
    </div>
  </div>
</section>